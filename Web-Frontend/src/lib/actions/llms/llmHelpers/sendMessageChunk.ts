export function sendMessageChunk(content: string) {
  if (content) {
    /*  mainWindow.webContents.send("messageChunk", content); */
  } else {
    console.log("This no work cause Chunk not chunky");
  }
}
